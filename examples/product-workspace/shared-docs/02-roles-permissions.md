# 角色与权限

## 角色定义

- 移动端用户：只读取面向自己的已发布内容。
- 内容管理员：创建、编辑、发布和撤回内容。

## 权限矩阵

| 能力 | 移动端用户 | 内容管理员 |
| --- | --- | --- |
| 查看已发布内容 | 允许 | 允许 |
| 查看草稿 | 不允许 | 允许 |
| 编辑与发布 | 不允许 | 允许 |

## 数据范围

- 移动端用户只能读取已发布内容。
- 内容管理员可以读取和维护全部内容状态。

## 越权处理

- 前端隐藏无权限入口。
- 后端仍须校验权限，拒绝越权请求并返回可识别的权限错误。
